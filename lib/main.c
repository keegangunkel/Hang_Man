#include <stdio.h>
#include "words.h"
#include "score.h"


int main() {
  //char* word = getRandomWordStr();
  //Word w = wordFromLetters(word);
  //free(word);

  // example of generting a word and free it
  //Word w = getHangmanWord();
  //printWord(w);
  //freeWord(w);
  check();

  return 0;
}
